# Sprint G2.5.3598 - Hide Internal Translation Endpoint Field

Goal:

Remove technical translation endpoint configuration from the Atlas Administration UI while keeping the DeepL endpoint available internally.

Deliverables:

* Removed the visible Translation API endpoint field from Atlas Administration
* Kept the DeepL translate endpoint as internal configuration
* Removed unused Admin UI labels for the endpoint field
* Documented that the endpoint is internal and not user-maintained

Status:

Completed.
