# Configuration

This section documents the configuration model of ATLAS.

It includes:

-   Configuration philosophy
-   Configuration suites
-   Manifest lifecycle

Configuration documents define how projects are configured consistently
across the framework.
